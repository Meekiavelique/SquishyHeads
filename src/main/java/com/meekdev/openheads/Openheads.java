// src/main/java/com/meekdev/openheads/Openheads.java

package com.meekdev.openheads;

import net.fabricmc.api.ModInitializer;

public class Openheads implements ModInitializer {

    @Override
    public void onInitialize() {
        // Cette partie est pour le code commun (client + serveur).
        // Comme notre fonctionnalité est uniquement côté client, on ne met rien ici.
    }
}